"use client";
import Link from "next/link";
import {usePathname} from "next/navigation";
import {useEffect,useRef,useState} from "react";
function localizedPath(pathname:string,locale:"ko"|"en"){const base=pathname.replace(/^\/en(?=\/|$)/,"")||"/";return locale==="en"?(base==="/"?"/en":`/en${base}`):base}
export default function SiteHeader(){const pathname=usePathname();const isEnglish=pathname==="/en"||pathname.startsWith("/en/");const prefix=isEnglish?"/en":"";const[languageOpen,setLanguageOpen]=useState(false);const[menuOpen,setMenuOpen]=useState(false);const languageRef=useRef<HTMLDivElement>(null);
useEffect(()=>{const outside=(e:MouseEvent)=>{if(!languageRef.current?.contains(e.target as Node))setLanguageOpen(false)};const escape=(e:KeyboardEvent)=>{if(e.key==="Escape"){setLanguageOpen(false);setMenuOpen(false)}};document.addEventListener("mousedown",outside);document.addEventListener("keydown",escape);return()=>{document.removeEventListener("mousedown",outside);document.removeEventListener("keydown",escape)}},[]);
const close=()=>setMenuOpen(false);return <header className="header"><div className="container nav"><Link className="brand" href={prefix||"/"} aria-label={isEnglish?"Logout home":"로그아웃 홈"}>{isEnglish?"Logout":"로그아웃"}<span className="cursor">_</span></Link><button className="mobile-menu-trigger" type="button" aria-expanded={menuOpen} aria-label={isEnglish?"Open menu":"메뉴 열기"} onClick={()=>setMenuOpen(v=>!v)}>{menuOpen?"닫기":"메뉴"}</button><nav className={`navlinks ${menuOpen?"open":""}`} aria-label={isEnglish?"Main navigation":"주요 메뉴"}><Link href={isEnglish?"/en":"/#start"} onClick={close}>{isEnglish?"Guide":"상황별 안내"}</Link><Link href={isEnglish?"/en#services":"/#services"} onClick={close}>{isEnglish?"Services":"서비스별 안내"}</Link><Link href={`${prefix}/prepare`} onClick={close}>{isEnglish?"Documents":"준비서류"}</Link><Link href={`${prefix}/about`} onClick={close}>{isEnglish?"About":"이야기"}</Link><Link href={`${prefix}/contact`} onClick={close}>{isEnglish?"Contact":"문의"}</Link><div className="language-menu" ref={languageRef}><button className="language-trigger" type="button" aria-label={isEnglish?"Choose language":"언어 선택"} aria-expanded={languageOpen} onClick={()=>setLanguageOpen(v=>!v)}><svg viewBox="0 0 24 24"><circle cx="12" cy="12" r="9"/><path d="M3 12h18M12 3c2.5 2.5 3.8 5.5 3.8 9S14.5 18.5 12 21c-2.5-2.5-3.8-5.5-3.8-9S9.5 5.5 12 3Z"/></svg></button>{languageOpen&&<div className="language-dropdown" role="menu"><Link href={localizedPath(pathname,"ko")} role="menuitem" className={!isEnglish?"active":""}>한국어</Link><Link href={localizedPath(pathname,"en")} role="menuitem" className={isEnglish?"active":""}>English</Link></div>}</div></nav></div></header>}
